<div>
    <div class="modal">
        <div class="modal__container">
            {{#if addUser}}
                {{{addUser}}}
            {{/if}}
            {{#if uploadFile}}
                {{{uploadFile}}}
            {{/if}}
        </div>
    </div>
</div>
